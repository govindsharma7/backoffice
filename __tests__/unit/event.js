// const models           = require('../../src/models');
// const fixtures         = require('../../__fixtures__/');

describe('Event', () => {
  describe('Scopes', () => {
    it('returns true', () => {
      expect(true).toEqual(true);
    });
  });
});
